# -*- mode: snippet; require-final-newline: nil -*-
# name: setup
# key: setup
# group: distribute
# --
from setuptools import setup

package = '${1:name}'
version = '${2:version}'

setup(name=package,
      version=version,
      description="${3:description}",
      url='${4:url}'$0)
